# --- GUI / Capture ---
PyQt5==5.15.11
mss==10.1.0
windows-capture==1.5.0         # WGC (Windows Graphics Capture) window-level capture
Pillow==12.1.1
numpy==2.4.3
PyYAML==6.0.3
pywin32==311

# --- AI / Inference ---
transformers==5.5.0
accelerate==1.13.0
bitsandbytes==0.49.2           # required for 4bit/8bit quantization

# torch / torchvision depend on CUDA build
# Example: pip install torch==2.11.0+cu128 torchvision==0.26.0+cu128 --index-url https://download.pytorch.org/whl/cu128
torch>=2.4.0
torchvision>=0.19.0

# --- Audio ---
scipy==1.15.3
pyaudiowpatch==0.2.12.8
